// Inserisci dei valori in un array e stampali a schermo con un ciclo for
let colori = [""];

// Stampiamo ogni colore usando un ciclo for
console.log("Ecco i colori disponibili:");
for (let i = 0; i < colori.length; i++) {
  console.log("- " + colori[i]);
}
